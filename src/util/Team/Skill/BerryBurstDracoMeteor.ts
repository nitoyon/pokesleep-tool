import { BerryBurstSkill } from "./BerryBurst";

/**
 * Berry Burst (Draco Meteor).
 *
 * Behaves identically to {@link BerryBurstSkill}; defined as its own class
 * so the skill map can distinguish this variant.
 */
export class BerryBurstDracoMeteorSkill extends BerryBurstSkill {}
